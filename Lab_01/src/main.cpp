#include "matrix.h"

int main(void)
{
    Matrix matrix(3, 3);
    for (int i = 0; i < 3; i++)
    {
        for (int j = 0; j < 3; j++)
        {
            // Working on operator overloading
        }
    }
    return 0;
}